## MangAfrik backend (Spring Boot)
# NEVER commit a real Neon password.
#
# Activate profile:
#   SPRING_PROFILES_ACTIVE=postgres
#
# Neon connection string (rotate if ever leaked):
#   DATABASE_URL=postgresql://USER:PASSWORD@HOST/DB?sslmode=require

SPRING_PROFILES_ACTIVE=postgres
DATABASE_URL=

